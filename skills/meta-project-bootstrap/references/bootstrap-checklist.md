# Bootstrap Checklist Reference

Stage 4 should confirm:

- GitHub repo coordinates
- visibility
- default branch
- local path

Stage 5 should confirm:

- core docs copied into business repo
- `docs/meta/project-manifest.yaml` exists
- `docs/meta/issue-map.yaml` exists
- business repo has an initial commit path available
